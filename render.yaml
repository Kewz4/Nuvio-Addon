services:
  - type: web
    name: nuvio-simple-streams
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: CONFIG_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
